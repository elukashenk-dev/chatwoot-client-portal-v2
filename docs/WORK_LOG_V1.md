# Work Log

Короткий лог реально выполненных шагов в `chatwoot-client-portal-v2`.

- Создан новый отдельный проект `v2` рядом со старым порталом, без копирования кода из `v1`.
- Зафиксированы стартовые документы: архитектура, журнал решений и план реализации.
- Собран базовый workspace проекта: root scripts, `pnpm`, frontend, backend, env/example и tooling.
- Поднят frontend foundation на `React + TypeScript + Vite + Tailwind CSS`.
- Добавлена зависимость `@preline/collapse` под выборочное использование `Preline`; текущие экраны пока собраны без `Preline`-компонентов.
- Создан backend scaffold на `Fastify + TypeScript + Zod` с базовым `health` route.
- Реализован первый живой auth UI-каркас.
- Сделан живой экран входа `/auth/login`.
- Добавлены базовые auth-компоненты: shell, brand block, footer, fields, password toggle, button, alert.
- Настроен frontend router с auth-маршрутами.
- Исправлено поведение футера на login screen при появлении ошибок валидации.
- Исправлены декоративные уголки в auth shell по утвержденному прототипу.
- Добавлен PWA foundation: manifest, service worker registration, standalone app metadata.
- Сгенерированы placeholder app icons для install flow и `apple-touch-icon`.
- Реализованы живые request-экраны вместо заглушек:
  - `/auth/register`
  - `/auth/password-reset/request`
- Для request-экранов добавлены локальная валидация, loading-state и error-state.
- Добавлены frontend тесты для login screen и request-экранов.
- Поднят backend auth foundation на `Fastify + PostgreSQL + Drizzle`: env для session/db, schema `portal_users` и `portal_sessions`, initial migration.
- Реализованы backend auth endpoints и DB-backed session/cookie auth:
  - `POST /api/auth/login`
  - `POST /api/auth/logout`
  - `GET /api/auth/me`
- Login screen переведен с mock submit на реальный backend auth flow: проверка текущей session, login, logout и dev proxy для `/api`.
- Email normalization приведена к единому правилу на backend: auth lookup теперь case-insensitive, новые portal users сохраняются в lowercase.
- Добавлен backend CLI-скрипт `pnpm --dir backend user:create` для создания локального тестового portal user без ручной правки БД.
- Step-документы по prototype-derived scope и future UI-slices переименованы в понятные active-temporary docs и добавлены в обязательный reading order.
- `reply state`, `quick emoji bar`, `message calendar`, `voice recording and send` подняты из step-docs в устойчивый продуктовый scope через `ARCHITECTURE`, `IMPLEMENTATION_PLAN` и `DECISIONS`.
- Из временных step-документов удален уже реализованный контекст; в них оставлен только remaining screen/slice context до будущего удаления файлов.
- Уточнено рабочее правило по старому `chatwoot-client-portal`: его можно читать как reference, но нельзя редактировать, копировать оттуда код или использовать его runtime/Postgres как часть работы над `v2`.
- Уточнено operational rule: локальное окружение `v2` запускает и останавливает только пользователь; агент не управляет окружением, а делает код, БД/таблицы/миграции и тесты в рамках задачи.
- Уточнено operational rule: первичную bootstrap-подготовку для первого запуска делает агент, но запуск и остановка сервисов `v2` по-прежнему выполняются только пользователем.
- Начат `Phase 2. Registration Flow`: добавлены backend registration request foundation, `verification_records`, Chatwoot contact lookup через account API, SMTP/Mailpit delivery для verification code и реальный `/auth/register` submit against backend API.
- Для registration foundation добавлены backend unit/integration tests, frontend request-page tests и migration под verification domain.
- Продвинут `Phase 2. Registration Flow`: добавлены backend verification confirm `POST /api/auth/register/verify`, attempt lifecycle и continuation token для следующего шага.
- Для registration flow подключены живой экран `/auth/register/verify`, `OtpInputGroup`, `ResendCodeRow`, session-backed flow state и переход в route `/auth/register/set-password`.
- Для `v2` подготовлен отдельный isolated `Postgres` bootstrap: `infra/postgres/compose.yaml`, package scripts `db:up/db:down/db:logs`, новые local env defaults и явный запрет использовать runtime-базу `Chatwoot` или БД старого портала.
- Для local Docker в WSL поправлены `db:*` scripts: isolated `Postgres` bootstrap теперь идет через `docker --context default`, чтобы не падать на `desktop-linux` named-pipe context.
- Isolated `Postgres` для `v2` поднят, новая база инициализирована, и миграции применены только против отдельной БД `v2`.
- `Phase 2. Registration Flow` доведен до set-password completion: добавлены `POST /api/auth/register/set-password`, создание `portal_user` только после successful verify + password set, consumption verification proof и live frontend form на `/auth/register/set-password`.
- Для registration set-password completion добавлены backend route/service tests, frontend page-flow tests и success/error UX без ручных заглушек.
- Прогнан живой registration happy path end-to-end: Chatwoot eligibility, Mailpit verification code, `/auth/register/verify`, `/auth/register/set-password` и вход новым паролем подтверждены на локальном окружении `v2`.
- Экран `/auth/register/verify` подогнан под prototype `frontend/screens/provgroup_auth_screen_02.html`: заголовок `Подтверждение Email`, intro с email на отдельной строке, filled OTP cells с light-gray background, per-cell focus вместо group highlight, disabled submit до полного кода без красной field-validation на неполном вводе, row `Изменить email`/`Отправить код повторно` и отдельный resend timer.
- Экран `/auth/register/set-password` подогнан под prototype `frontend/screens/provgroup_auth_screen_04.html`: title/intro `Создание пароля`, password requirements card с live rule states, button `Сохранить пароль`, prototype-aligned placeholders и нижние actions `Вернуться назад` / `Перейти ко входу`.
- Создан `docs/Findings/` как review findings registry активных рисков, зафиксирован workflow в `AGENTS.md` и `DECISIONS.md`, а open Registration Flow findings разложены в отдельные файлы `F-REG-001` ... `F-REG-003`.
- Закреплено правило: после фикса и проверки finding его markdown-файл удаляется из `docs/Findings/`; уже закрытый `F-REG-004` удален из registry.
- Удален временный `docs/steps/prototype-reference-and-chat-scope.md`: его prototype rules и confirmed chat scope уже перенесены в устойчивые документы.
- `Phase 3. Password Reset` реализован как отдельный backend-owned flow: request, verify, continuation-token set-password, `purpose = password_reset`, generic request response без disclosure account existence и SMTP/Mailpit delivery для active portal users.
- Password reset completion обновляет password hash, consumes reset proof и удаляет существующие portal sessions пользователя, чтобы старый пароль и старые sessions не оставались рабочим путем.
- Frontend password reset переведен с mock request на живой backend flow:
  - `/auth/password-reset/request`
  - `/auth/password-reset/verify`
  - `/auth/password-reset/set-password`
- Для reset flow добавлены session-backed flow state, shared `PasswordRulesCard`, success/error UX и тесты на request/verify/set-password.
- `docs/steps/remaining-screen-map-and-ui-slices.md` сокращен: password reset routes и set-password UI больше не числятся remaining context.
- Проверки проходили на выполненных шагах: `lint`, `typecheck`, `test`, `build`.
- Для isolated `Postgres` bootstrap дополнительно проверен `docker compose config`.
- Для `Phase 3. Password Reset` пройдены актуальные проверки: `lint`, frontend `typecheck`, `test`, `build`.
- Закрыты Registration Flow findings `F-REG-001`, `F-REG-002` и `F-REG-003`: registration request/verify переведены на per-email transaction scoped lock, parallel request больше не создает несколько active pending codes, parallel invalid verify не теряет attempts count, а pending verification во время resend cooldown продолжается без зависимости от Chatwoot availability.
- Закрытые finding-файлы `F-REG-001`, `F-REG-002` и `F-REG-003` удалены из `docs/Findings/` по правилу active-only registry.
- Для закрытия Registration Flow findings пройдены проверки: `pnpm --dir backend test`, `pnpm --dir backend lint`, `pnpm --dir backend build`.
- Во время закрытия Registration Flow findings найден adjacent auth-sensitive risk `F-RESET-001`: password reset verify имеет аналогичный attempts race; finding зафиксирован отдельно и не исправлялся в этом шаге.
- Закрыт `F-RESET-001`: password reset verify переведен на per-email transaction scoped lock, invalid attempts и successful verify теперь выполняются через transaction executor, добавлен parallel invalid reset verify regression test.
- Закрытый finding-файл `F-RESET-001` удален из `docs/Findings/` по правилу active-only registry.
- Для закрытия `F-RESET-001` пройдены проверки: `pnpm --dir backend test -- src/modules/password-reset/service.test.ts`, `pnpm --dir backend lint`, `pnpm --dir backend build`.
- Проведен review `Phase 2. Registration Flow` после фикса concurrency findings; новые active findings зафиксированы в `docs/Findings/F-P2-001` ... `F-P2-005`.
- Закрыты Phase 2 findings `F-P2-001` ... `F-P2-005`: добавлена durable таблица `portal_user_contact_links`, registration completion теперь сохраняет link `portal_user -> Chatwoot contact`, request verification больше не отдает reusable success для in-flight email delivery, resend API сообщает `delivery`, backend password policy выровнен с frontend и registration session-state чистит expired request/verification.
- Закрытые finding-файлы `F-P2-001` ... `F-P2-005` удалены из `docs/Findings/` по правилу active-only registry.
- Новая migration `0003_regular_giant_girl.sql` применена к isolated `Postgres` v2 через `pnpm --dir backend db:migrate`.
- Для закрытия Phase 2 findings пройдены проверки: `pnpm --dir backend test`, `pnpm --dir frontend test`, `pnpm --dir backend lint`, `pnpm --dir backend build`, `pnpm --dir frontend lint`, `pnpm --dir frontend typecheck`, `pnpm --dir frontend build`, `pnpm --dir backend db:migrate`.
- Проведен post-fix review `Phase 2. Registration Flow`; найден active finding `F-P2R-001` по cleanup `sending` verification при SMTP configuration error.
- Закрыт `F-P2R-001`: registration delivery failure cleanup теперь выполняется и при `SmtpEmailDeliveryConfigurationError`, новая `sending` verification удаляется или предыдущая pending verification восстанавливается до выброса user-facing error.
- Закрытый finding-файл `F-P2R-001` удален из `docs/Findings/` по правилу active-only registry.
- Для закрытия `F-P2R-001` пройдены проверки: `pnpm --dir backend exec vitest run src/modules/registration/service.test.ts`, `pnpm --dir backend test`, `pnpm --dir backend lint`, `pnpm --dir backend build`.
- Проведен repeat light review `Phase 2. Registration Flow`: критичных или средних новых рисков не найдено; low UX-consistency finding `F-P2R2-001` зафиксирован в `docs/Findings/`.
- Для repeat light review `Phase 2` пройдены targeted checks: `pnpm --dir backend exec vitest run src/modules/registration/service.test.ts src/app.test.ts`, `pnpm --dir frontend exec vitest run src/features/auth/pages/RequestPages.test.tsx src/features/auth/pages/LoginPage.test.tsx`.
- `Phase 4. Protected App Shell` реализован: добавлены frontend auth-session bootstrap через `/api/auth/me`, protected routes для `/app/*`, app shell, logout UX и protected empty state на `/app/chat`.
- Login flow переведен на новый auth-session слой: успешный вход ведет в `/app/chat`, существующая session с `/auth/login` открывает app shell, logout возвращает на login.
- Для protected shell добавлены frontend regression tests: unauthorized `/app/chat` возвращается на login, authenticated session открывает shell, bootstrap error показывает retry-state.
- Для `Phase 4. Protected App Shell` пройдены проверки: `pnpm --dir frontend exec vitest run src/features/auth/pages/LoginPage.test.tsx`, `pnpm --dir frontend test`, `pnpm --dir frontend lint`, `pnpm --dir frontend typecheck`, `pnpm --dir frontend build`.
- Проведен review `Phase 4. Protected App Shell`; найден active finding `F-P4-001`: authenticated session редиректится из `/auth/login`, но остальные auth routes остаются доступными и могут оставить авторизованного пользователя вне portal shell.
- Для review `Phase 4` пройдены проверки: `pnpm --dir frontend test`, `pnpm --dir frontend lint`, `pnpm --dir frontend typecheck`.
- Закрыт `F-P4-001`: добавлен `PublicAuthRoute`, все public auth routes теперь редиректят authenticated session в `/app/chat`, а unauthenticated/error states продолжают открывать auth flow.
- Закрытый finding-файл `F-P4-001` удален из `docs/Findings/` по правилу active-only registry.
- Для закрытия `F-P4-001` пройдены проверки: `pnpm --dir frontend exec vitest run src/features/auth/pages/LoginPage.test.tsx src/features/auth/pages/RequestPages.test.tsx`, `pnpm --dir frontend test`, `pnpm --dir frontend lint`, `pnpm --dir frontend typecheck`, `pnpm --dir frontend build`.
- Проведен post-fix review закрытия `F-P4-001`: новых Phase 4 shell/auth-routing findings не найдено.
- Для post-fix review `F-P4-001` пройдены проверки: `pnpm --dir frontend exec vitest run src/features/auth/pages/LoginPage.test.tsx src/features/auth/pages/RequestPages.test.tsx`, `pnpm --dir frontend lint`, `pnpm --dir frontend typecheck`, `pnpm --dir frontend build`.
- Закрыт `F-P2R2-001`: shared frontend password rule-state теперь считает length по `password.trim().length`, как registration/password-reset form validation и backend password policy.
- Для padded short password добавлены frontend regression tests на registration set-password и password reset set-password.
- Закрытый finding-файл `F-P2R2-001` удален из `docs/Findings/` по правилу active-only registry.
- Для закрытия `F-P2R2-001` пройдены проверки: `pnpm --dir frontend exec vitest run src/features/auth/pages/RequestPages.test.tsx`, `pnpm --dir frontend test`, `pnpm --dir frontend lint`, `pnpm --dir frontend typecheck`, `pnpm --dir frontend build`.

## Recommended Next Step

- Переходить к `Phase 5. Chat Read Model`: backend `chat-context`, authoritative primary conversation resolution, `GET /api/chat/context`, `GET /api/chat/messages`, transcript rendering и controlled ready/not_ready states.
